# Image Processor

A namespace for lightweight, offline-first image processing tools built with Python. Each tool runs locally with minimal dependencies — no external APIs, no internet required.

## Tools

- **[ALPR Service](./alpr-service/)** — Automatic licence plate recognition using local ONNX models. Directory watcher + REST API.



